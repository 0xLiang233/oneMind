export function SearchPage() {
  return (
    <section className="page">
      <div className="search-empty-state">
        <div className="notes-panel-title">搜索笔记</div>
        <p>这里后续会并入原型中的命令面板与全文检索模型，当前先保留为场景入口。</p>
      </div>
    </section>
  )
}
