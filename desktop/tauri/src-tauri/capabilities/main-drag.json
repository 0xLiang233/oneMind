{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-drag",
  "description": "Allow window dragging from the local main renderer only.",
  "webviews": ["main"],
  "permissions": ["core:window:allow-start-dragging"]
}
