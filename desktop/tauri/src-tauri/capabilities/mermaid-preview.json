{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "mermaid-preview",
  "description": "Local diagram preview windows; no updater or process permissions",
  "windows": ["mermaid-preview-*"],
  "permissions": ["core:event:default"]
}
